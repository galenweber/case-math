import { StyleSheet } from 'react-native';

const styles = {
  container: {
    flex: 1,
  },
  tile: {
    backgroundColor: '#00C853',
    color: 'red',
  },
}

export default StyleSheet.create(styles);
